/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Enables use of `unstable_cache` and Route Handler caching primitives.
  },
};

export default nextConfig;
